{
  "name": "quiz-dev-runner",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "prisma": "^6.9.0"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0"
  }
}
